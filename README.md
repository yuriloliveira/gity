# gity
Git CLI to simplify some of the most common uses of git

## Build
### MacOS
```bash
cargo build --release --target aarch64-apple-darwin
```

### Windows
```bash
cargo build --release --target x86_64-pc-windows-msvc
```

### Available targets
```bash
rustc --print target-list
```